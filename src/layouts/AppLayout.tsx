import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { BarChart3, BriefcaseBusiness, CheckSquare, Database, FileText, Home, Layers, Settings, ShieldCheck } from 'lucide-react';
import { useAppStore } from '../store';
import { VerificationDrawer } from '../components/ui';
const nav=[['/','工作台',Home],['/reporting','专题报送',FileText],['/progress','进展与异常',BarChart3],['/compliance','协议履约',ShieldCheck],['/portfolio','项目库',BriefcaseBusiness],['/documents','材料中心',Layers],['/tasks','投资经理待办',CheckSquare],['/settings','数据与核验设置',Settings]] as const;
export default function AppLayout(){const navg=useNavigate();const demo=useAppStore(s=>s.demo);const setDemo=useAppStore(s=>s.setDemo);return <div className="app"><aside className="side"><div className="brand">未来基金投后管理智能平台</div><div className="sub">Future Fund Portfolio Intelligence</div><nav className="nav">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="badge blue" style={{marginTop:16}}>Demo Mode · Local Rule Engine</div></aside><main className="main"><header className="top"><input className="search" placeholder="全局搜索项目、材料、条款、来源..."/><div className="toolbar"><span className="muted">数据更新：2026-07-30</span><button className="btn primary" onClick={()=>{setDemo(!demo);navg('/reporting')}}>{demo?'退出演示模式':'进入演示模式'}</button><span className="badge blue">张经理</span></div></header><section className="content"><Outlet/></section></main><VerificationDrawer/></div>}