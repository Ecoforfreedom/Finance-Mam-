import { describe, expect, it } from 'vitest';import { parseNaturalLanguage, runQuery } from './rules';
describe('本地规则引擎',()=>{it('解析量子+A轮及以前+上海落地',()=>{const c=parseNaturalLanguage('哪些项目同时满足量子科技、A轮及以前、上海落地？');expect(c.quantum).toBe(true);expect(c.stageMax).toBe('A轮');expect(c.shanghaiMode).toBe('landing');});it('上海实际经营口径会改变结果',()=>{const a=runQuery(parseNaturalLanguage('量子科技、A轮及以前、上海落地'));const b=runQuery(parseNaturalLanguage('量子科技、A轮及以前、上海开展实际经营'));expect(a.included.length+a.pending.length).toBeGreaterThanOrEqual(b.included.length);});it('AI for Science返回纳入和待确认',()=>{const r=runQuery(parseNaturalLanguage('哪些项目属于AI for Science？'));expect(r.included.length).toBeGreaterThan(0);expect(r.pending.length).toBeGreaterThan(0);});});